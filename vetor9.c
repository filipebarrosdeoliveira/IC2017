#include <stdio.h>
#include <stdlib.h>

main(){
char vetor[80],i;

	 printf("\n Digite uma frase qualquer: ");
	 fflush(stdin);
	 gets(vetor);
	 printf("\n Sua frase : %s\n",vetor);
	 system("pause");
	 }
