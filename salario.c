#include <stdio.h>

int main () {

	float salario;
	scanf ("%f", & salario);

	if ( salario <= 500 )
		printf ("Receberá aumento \n Valor a ser recebido será de %f \n", salario = ((salario * 0.3) + salario));

	else 
		printf ("Não receberá aumento");

return 0;

}
