#include <stdio.h>

int main () {

int area, base,altura;

printf ("Entre com a base \n");
scanf ("%d", & base);
printf ("Entre com a altura \n");
scanf ("%d", & altura);

printf("%d \n", (area =  (base*altura)/2));

return 0;
}
